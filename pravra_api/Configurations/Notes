dotnet add package MongoDB.Driver
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer

JWT Secret Key - Convert.ToBase64String(Guid.NewGuid().ToByteArray());

Added cors in both for local debugging and allowed the url in app service cors settings by adding url of ui in api app service configurations.